type ButtonPageProps = {};
const ButtonPage = ({}: ButtonPageProps) => {
  return (
    <div>
      <pre>
        {`.btn-icon {
    @apply text-white p-1 rounded-sm inline-flex items-center justify-center;
  }
  .btn-icon svg {
    @apply h-5;
  }`}
      </pre>
    </div>
  );
};

export default ButtonPage;
